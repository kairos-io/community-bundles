apiVersion: apps/v1
kind: Deployment
metadata:
  name: operator-kairos-operator
  namespace: operator-system
spec:
  template:
    spec:
      containers:
      - name: manager
        env:
        - name: @ENV_NAME@
          value: "@ENV_VALUE@"
